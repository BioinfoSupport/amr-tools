{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
		"limit_long_reads_len": {
			"type": "integer",
			"description": "Limit the number input reads to this maximum total length (in bp)",
			"default": -1
		},
		"limit_short_reads_len": {
			"type": "integer",
			"description": "Limit the number input reads to this maximum total length (in bp)",
			"default": -1
		}		
  }
}

